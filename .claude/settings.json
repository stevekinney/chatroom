{
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Edit|Write",
				"hooks": [
					{
						"type": "command",
						"command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in */CLAUDE.md|CLAUDE.md) jq -n '{systemMessage: \"Reminder: CLAUDE.md changed — verify AGENTS.md still points at it, and check whether new content needs anything called out separately for Codex or other agent CLIs.\", hookSpecificOutput: {hookEventName: \"PostToolUse\", additionalContext: \"CLAUDE.md was just edited. AGENTS.md is a two-line pointer to CLAUDE.md — confirm it still resolves correctly, and flag anything Codex-specific that this change should surface.\"}}' ;; esac; } 2>/dev/null"
					},
					{
						"type": "command",
						"command": "jq -r '[.session_id, (.tool_response.filePath // .tool_input.file_path)] | join(\"|\")' | { IFS='|' read -r sid f; case \"$f\" in *.json|*.md|*.yml|*.yaml|.claude/*|.vscode/*|*.config.*|.env*|*.lock|README*|.prettierrc*|.eslintrc*|tsconfig*) : ;; *) rm -f \"/tmp/claude-cinder-nudge-${sid}\" ;; esac; } 2>/dev/null"
					}
				]
			}
		],
		"Stop": [
			{
				"hooks": [
					{
						"type": "command",
						"command": "jq -r '.session_id' | { read -r sid; f=\"/tmp/claude-cinder-nudge-${sid}\"; if [ ! -f \"$f\" ]; then touch \"$f\"; ex=(':!*.json' ':!*.md' ':!*.yml' ':!*.yaml' ':!.claude' ':!.vscode' ':!*.config.*' ':!.env*' ':!*.lock' ':!README*' ':!.prettierrc*' ':!.eslintrc*' ':!tsconfig*'); diff=$(git diff HEAD -- . \"${ex[@]}\" 2>/dev/null | head -c 20000); untracked=$(git status --short -- . \"${ex[@]}\" 2>/dev/null | grep '^??' | sed 's/^?? //'); prompt=\"Before wrapping up: review the diff and new files below for hacks, workarounds, or silent accommodations around @lostgradient/cinder or conversationalist (undocumented behavior, awkward import ordering, manual peer-dependency installs, forced type casts, style/layer ordering fixes, etc.). If you find one, file an issue against stevekinney/cinder via gh issue create per CLAUDE.md convention — check it is not a duplicate of #753/#754 first — rather than leaving it undocumented.\"; jq -n --arg prompt \"$prompt\" --arg diff \"$diff\" --arg untracked \"$untracked\" '{hookSpecificOutput: {hookEventName: \"Stop\", additionalContext: ($prompt + \"\\n\\nUncommitted diff (excludes config/docs, truncated at 20000 chars):\\n\" + (if ($diff|length) > 0 then $diff else \"(none)\" end) + \"\\n\\nUntracked new files (excludes config/docs):\\n\" + (if ($untracked|length) > 0 then $untracked else \"(none)\" end))}}'; fi; } 2>/dev/null"
					}
				]
			}
		]
	}
}
